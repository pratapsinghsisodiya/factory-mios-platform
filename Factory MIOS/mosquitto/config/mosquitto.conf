# Mosquitto broker config for Factory MIOS
listener 1883
protocol mqtt

listener 9001
protocol websockets

# Dev default: allow anonymous. For production, set to false and configure
# a password_file / per-tenant ACLs. See docs/SECURITY.md.
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
